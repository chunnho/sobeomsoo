import { useEffect, useState } from 'react';
var useMousePressed = function (targetRef) {
    var _a = useState(false), isMousePressed = _a[0], setIsMousePressed = _a[1];
    var handleMouseDown = function () {
        setIsMousePressed(true);
    };
    var handleMouseUp = function () {
        setIsMousePressed(false);
    };
    useEffect(function () {
        var targetElement = 'current' in targetRef ? targetRef.current : targetRef;
        if (!targetElement)
            return;
        targetElement.addEventListener('mousedown', handleMouseDown);
        document.addEventListener('mouseup', handleMouseUp);
        return function () {
            targetElement.removeEventListener('mousedown', handleMouseDown);
            document.removeEventListener('mouseup', handleMouseUp);
        };
    }, [targetRef]);
    return isMousePressed;
};
export default useMousePressed;
//# sourceMappingURL=useMousePressed.js.map